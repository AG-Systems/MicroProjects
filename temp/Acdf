#include <iostream>
#include <string>
#include <time.h>

int main()
{
    std::string sentence;
    char input;
    std::string dictionary[4] = {"the","problem","th"};
    for(int z = 0; z < 1; z--)
    {
        std::cin >> input;
        
        if (input == "\n")
        {
            break;    
        }
        
    
}
