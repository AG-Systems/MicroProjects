#include <iostream>

int main()
{
    int boxes[3] = {'7', '21', '14'}
    
    for(int z = 0; z < 10; z++)
    {
        for ( int i=1;  i < sizeof(array)/sizeof(array[0]);  ++i )
        if ( array[i] < smallest )
             smallest = array[i] ;

    cout << smallest << '\n' ;           
    }
}
